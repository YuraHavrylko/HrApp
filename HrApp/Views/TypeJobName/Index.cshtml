@model System.Collections.Generic.IEnumerable<HrApp.Models.TypeJobsName>

@{
    ViewBag.Title = "Language level name";
}

<div class="content-wrapper">
    <section class="content-header">
        <h1>
            Type job settings
        </h1>
        <ol class="breadcrumb">
            <li><a href="/Home"><i class="fa fa-dashboard"></i> Home</a></li>
            <li>Settings</li>
            <li class="active">Type job settings</li>
        </ol>
    </section>

    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="box-body">
                        <table class="table table-bordered">
                            <tr>
                                <th>Name of type job</th>
                                <th style="width: 40px"></th>
                                <th style="width: 40px"></th>
                            </tr>
                            @foreach (var type in Model)
                            {
                                <tr>
                                    <td>@type.TypeJobName</td>
                                    <td>@Html.ActionLink("Edit", "Edit", "TypeJobName", new { id = type.TypeJobId }, new { })</td>
                                    <td>@Html.ActionLink("Delete", "Delete", "TypeJobName", new { typeJobName = type.TypeJobId }, new { })</td>
                                </tr>
                            }
                            <tr>

                        </table>
                    </div>
                    <div class="box-footer clearfix">
                        @Html.ActionLink("Create new type job", "Create", "TypeJobName", null, new { @class = "btn btn-sm btn-info btn-raised btn-flat" })
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
