@using System.Web.Mvc.Html
@using HrApp.Models
@model HrApp.Models.Language

@{
    ViewBag.Title = "Add person language";
    List<TypeLanguage> list = ViewBag.LanguageName;
    List<LanguageLevel> levels = ViewBag.LanguageLevel;
}
<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            @using (Html.BeginForm())
            {
                @Html.ValidationSummary(true)
                <fieldset>
                    <legend>Add person language</legend>
                    <div class="form-group">
                        @Html.LabelFor(model => model.LanguageName)
                        @Html.DropDownListFor(model => model.LanguageName, new SelectList(list.Select(language => language.LanguageName)), "", new { @class = "form-control" })
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.LanguageName)
                        @Html.DropDownListFor(model => model.LanguageLevelName, new SelectList(levels.Select(language => language.LanguageLevelName)), "", new { @class = "form-control" })
                    </div>
                    @Html.HiddenFor(model => model.PersonId)
                    <div class="form-group">
                        <p>
                            <input type="submit" value="Create" class="btn btn-success btn-block" />
                        </p>
                    </div>
                </fieldset>
            }
            @Html.ActionLink("Back to Person", "FullInformation", "Home", new { id = Model.PersonId }, null)
        </div>
    </div>
</div>